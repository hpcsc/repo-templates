questions:
  - name: GoVersion
    prompt:
      message: "Go version"
    validate:
      required: true

messages:
  post: |
    # All set

presets:
  example:
    Project: go-worker-project
    GoVersion: "1.25.4"
